==
'
cat /etc/redhat-release

printf '\n=== 현재 Crypto Policy ===\n'
update-crypto-policies --show

printf '\n=== Apache 상태 ===\n'
systemctl is-active httpd</code></pre><div class="command-explain" id="explain-pqc-pre"><div class="explain-title">필요할 때만 펼쳐보는 명령어 설명</div><dl><dt><code>cat /etc/redhat-release</code></dt><dd>RHEL 10.2 환경인지 확인합니다.</dd><dt><code>update-crypto-policies --show</code></dt><dd>현재 정책이 <code>FUTURE</code>인지 확인합니다.</dd><dt><code>systemctl is-active httpd</code></dt><dd>앞 실습에서 복구한 Apache가 <code>active</code>인지 확인합니다.</dd></dl></div></div><div class="table-scroll"><table class="verify"><thead><tr><th>확인 항목</th><th>정상 결과</th><th>다음 단계</th></tr></thead><tbody><tr><td>RHEL 버전</td><td>RHEL 10.2</td><td>RHEL 10.2 PQC 동작 확인 환경</td></tr><tr><td>Crypto Policy</td><td><code>FUTURE</code></td><td>RHEL 10.2 FUTURE 정책 확인</td></tr><tr><td>Apache</td><td><code>active</code></td><td>TLS 1.3 연결 테스트 진행</td></tr></tbody></table></div><h3>1-2. 실제 TLS 1.3 연결에서 Hybrid PQC 확인</h3><div class="code-card" data-lang="shell"><div class="code-head"><span>Shell · Hybrid ML-KEM 확인</span><div class="code-actions"><button aria-controls="explain-pqc-kem" aria-expanded="false" class="explain-toggle" type="button">명령어 설명</button><button class="copy" type="button">복사</button></div></div><pre><code>openssl s_client \
  -connect localhost:443 \
  -tls1_3 \
  &lt;/dev/null 2&gt;&amp;1 \
  | grep -E 'Negotiated TLS1.3 group|Peer signature type'</code></pre><div class="command-explain" id="explain-pqc-kem"><div class="explain-title">필요할 때만 펼쳐보는 명령어 설명</div><dl><dt><code>-connect localhost:443</code></dt><dd>현재 Apache HTTPS 포트에 접속합니다.</dd><dt><code>-tls1_3</code></dt><dd>TLS 1.3 핸드셰이크로 제한합니다.</dd><dt><code>Peer signature type</code></dt><dd>서버가 자신을 증명할 때 사용한 서명 방식을 보여줍니다.</dd><dt><code>Negotiated TLS1.3 group</code></dt><dd>클라이언트와 서버가 공유 비밀을 설정할 때 협상한 Key Exchange 그룹을 보여줍니다.</dd></dl></div></div><div class="code-card" data-lang="text"><div class="code-head"><span>정상 결과 예시</span></div><pre><code>Peer signature type: rsa_pss_rsae_sha256
Negotiated TLS1.3 group: X25519MLKEM768</code></pre></div><div class="output-explain"><div><h3>서버 인증</h3><code>rsa_pss_rsae_sha256</code><p>앞에서 만든 RSA 3072 인증서를 사용해 서버가 자신을 증명합니다. 현재 서버 인증은 아직 기존 RSA 방식입니다.</p></div><div><h3>공유 비밀 설정</h3><code>X25519MLKEM768</code><p>기존 X25519와 PQC인 ML-KEM-768을 함께 사용한 Hybrid Key Exchange입니다. 이 줄이 PQC 확인의 핵심입니다.</p></div></div><div class="callout ok"><strong>PQC 핵심 검증 성공</strong><p><code>Negotiated TLS1.3 group: X25519MLKEM768</code>이 보이면, RHEL 10.2의 FUTURE 정책 아래에서 현재 Apache TLS 연결이 <strong>기존 방식 + PQC 방식의 Hybrid Key Exchange</strong>를 사용한다는 것을 확인한 것입니다.</p></div><div class="callout info"><strong>두 기술을 혼동하지 마세요.</strong><p><strong>ML-KEM은 통신 데이터를 직접 암호화하는 알고리즘이 아니라 공유 비밀을 설정하는 기술</strong>입니다. 핸드셰이크가 끝난 뒤 실제 HTTPS 데이터는 AES 같은 효율적인 대칭키 알고리즘으로 암호화됩니다.</p></div><div class="callout ok"><strong>이 실습에서 가장 집중해서 볼 값</strong><p><code>Negotiated TLS1.3 group: X25519MLKEM768</code>이 핵심입니다. <code>Peer signature type: rsa_pss_rsae_sha256</code>은 서버 인증이 RSA라는 뜻이고, <code>X25519MLKEM768</code>은 <strong>기존 X25519 + PQC ML-KEM-768</strong>을 함께 사용하는 Hybrid Key Exchange라는 뜻입니다.</p></div></section><section id="s2-pqc"><div class="section-kicker">STEP 8 · OPTIONAL 5 MIN</div><h2>선택 확장 — ML-DSA 전자서명 체험</h2><p class="lead">Step 1의 Hybrid ML-KEM 확인을 완료한 뒤 시간이 남으면 진행합니다. ML-DSA가 PQC 전자서명 알고리즘이라는 점을 파일 서명과 변조 탐지로 확인합니다.</p><h3>2-1. ML-DSA-65 키 생성 및 정상 서명 검증</h3><div class="callout info"><strong>ML-DSA는 무엇을 확인하나요?</strong><p>ML-DSA는 “누가 서명했고, 서명 이후 데이터가 바뀌지 않았는가?”를 확인합니다. 데이터를 숨기는 암호화가 아니라 <strong>신뢰성과 무결성을 증명하는 전자서명</strong>입니다.</p></div><div class="code-card" data-lang="shell"><div class="code-head"><span>Shell · ML-DSA-65 서명 및 검증</span><div class="code-actions"><button aria-controls="explain-mldsa" aria-expanded="false" class="explain-toggle" type="button">명령어 설명</button><button class="copy" type="button">복사</button></div></div><pre><code>mkdir -p /root/pqc-lab
cd /root/pqc-lab

printf 'Hello RHEL 10 PQC\n' &gt; message.txt

openssl genpkey \
  -algorithm mldsa65 \
  -out mldsa65-private.pem

openssl pkey \
  -in mldsa65-private.pem \
  -pubout \
  -out mldsa65-public.pem

openssl dgst \
  -sign mldsa65-private.pem \
  -out message.sig \
  message.txt

openssl dgst \
  -verify mldsa65-public.pem \
  -signature message.sig \
  message.txt</code></pre><div class="command-explain" id="explain-mldsa"><div class="explain-title">필요할 때만 펼쳐보는 명령어 설명</div><dl><dt><code>openssl genpkey -algorithm mldsa65</code></dt><dd>ML-DSA-65 개인키를 생성합니다.</dd><dt><code>openssl pkey ... -pubout</code></dt><dd>개인키에서 검증용 공개키를 추출합니다.</dd><dt><code>openssl dgst -sign</code></dt><dd>개인키로 <code>message.txt</code>에 전자서명합니다.</dd><dt><code>openssl dgst -verify</code></dt><dd>공개키로 서명이 유효하고 파일이 변경되지 않았는지 검증합니다.</dd></dl></div></div><div class="code-card" data-lang="text"><div class="code-head"><span>정상 결과</span></div><pre><code>Verified OK</code></pre></div><h3>2-2. 파일 변경 후 검증 실패 확인</h3><div class="code-card" data-lang="shell"><div class="code-head"><span>Shell · 변조 탐지 확인</span><div class="code-actions"><button aria-controls="explain-mldsa-tamper" aria-expanded="false" class="explain-toggle" type="button">명령어 설명</button><button class="copy" type="button">복사</button></div></div><pre><code>cp message.txt message-original.txt
printf 'Modified after signing\n' &gt;&gt; message.txt

openssl dgst \
  -verify mldsa65-public.pem \
  -signature message.sig \
  message.txt

mv -f message-original.txt message.txt</code></pre><div class="command-explain" id="explain-mldsa-tamper"><div class="explain-title">필요할 때만 펼쳐보는 명령어 설명</div><dl><dt><code>printf ... &gt;&gt; message.txt</code></dt><dd>서명된 파일에 내용을 추가하여 서명 이후 변경 상황을 만듭니다.</dd><dt><code>openssl dgst -verify</code></dt><dd>기존 서명과 변경된 파일이 일치하지 않아 검증이 실패하는지 확인합니다.</dd><dt><code>mv -f message-original.txt message.txt</code></dt><dd>테스트 후 원본 파일을 복원합니다.</dd></dl></div></div><div class="callout ok"><strong>ML-DSA에서 집중할 결과</strong><p>원본 파일에서는 <code>Verified OK</code>, 변경된 파일에서는 <code>Verification failure</code>가 나타나면 ML-DSA 전자서명이 서명자 확인과 변조 탐지에 사용된다는 것을 체험한 것입니다.</p></div></section><section id="trouble"><div class="section-kicker">TROUBLESHOOT</div><h2>추가 실습 문제 해결</h2><p class="lead">TLS 결과가 보이지 않으면 “PQC 미지원”이라고 바로 결론내리지 말고, 먼저 Apache와 TLS 핸드셰이크 상태를 확인합니다.</p><h3>출력이 비어 있거나 no peer certificate available이 표시될 때</h3><div class="code-card" data-lang="shell"><div class="code-head"><span>Shell · Apache / 443 / SSL 로그 확인</span><div class="code-actions"><button aria-controls="explain-pqc-trouble" aria-expanded="false" class="explain-toggle" type="button">명령어 설명</button><button class="copy" type="button">복사</button></div></div><pre><code>printf '=== Apache 상태 ===\n'
systemctl status httpd.service --no-pager

printf '\n=== 443 LISTEN 확인 ===\n'
ss -ltnp | grep ':443'

printf '\n=== SSL 오류 로그 ===\n'
tail -20 /var/log/httpd/ssl_error_log

printf '\n=== 전체 TLS 출력 ===\n'
openssl s_client -connect localhost:443 -tls1_3 &lt;/dev/null</code></pre><div class="command-explain" id="explain-pqc-trouble"><div class="explain-title">필요할 때만 펼쳐보는 명령어 설명</div><dl><dt><code>systemctl status httpd.service</code></dt><dd>Apache가 실제로 실행 중인지 확인합니다.</dd><dt><code>ss -ltnp | grep ':443'</code></dt><dd>HTTPS 포트가 LISTEN 중인지 확인합니다.</dd><dt><code>tail -20 /var/log/httpd/ssl_error_log</code></dt><dd>인증서 또는 Crypto Policy 관련 오류를 확인합니다.</dd><dt><code>openssl s_client ...</code></dt><dd>grep으로 내용을 숨기지 않고 TLS 핸드셰이크 전체 출력을 확인합니다.</dd></dl></div></div><div class="callout warn"><strong>no peer certificate available의 의미</strong><p>클라이언트가 TLS 핸드셰이크 중 서버 인증서를 전달받기 전에 연결이 실패했다는 뜻입니다. PQC 성공 메시지가 아니며, Apache 미기동·443 미LISTEN·인증서 오류·알고리즘 호환성 문제를 먼저 확인해야 합니다.</p></div></section><div class="callout ok"><strong>한 문장으로 정리</strong><p><strong>본 실습은 Crypto Policy가 Apache에 실제로 강제되는 과정을 먼저 경험하고, 같은 TLS 연결에서 ML-KEM 기반 Hybrid PQC Key Exchange를 확인하는 실습입니다.</strong> ML-DSA는 시간이 남을 때 전자서명 역할을 별도로 체험하는 확장 단계입니다.</p></div><section id="check"><div class="callout warn"><strong>추가 실습 완료 기준</strong><br/>필수 성공 기준은 <code>X25519MLKEM768</code> 확인입니다. ML-DSA는 시간이 남을 때 진행하는 선택 확장입니다.</div>
<div class="section-kicker">CHECK</div>
<h2>PQC 추가 실습 최종 체크리스트</h2>
<div class="progress"><span></span></div><div class="progress-text">0 / 9</div>
<div class="checklist"><label><input type="checkbox"/>RHEL 10.2 환경임을 확인했다.</label><label><input type="checkbox"/>현재 System-wide Cryptographic Policy가 FUTURE임을 확인했다.</label><label><input type="checkbox"/>Apache가 active 상태임을 확인했다.</label><label><input type="checkbox"/>TLS 1.3 연결에서 Peer signature type이 RSA 서명임을 확인했다.</label><label><input type="checkbox"/>TLS 1.3 연결에서 X25519MLKEM768을 확인했다.</label><label><input type="checkbox"/>X25519MLKEM768이 기존 X25519와 PQC ML-KEM-768을 함께 사용하는 Hybrid Key Exchange임을 이해했다.</label><label><input type="checkbox"/>ML-KEM은 실제 HTTPS 데이터를 직접 암호화하는 것이 아니라 공유 비밀 설정에 사용된다는 점을 이해했다.</label><label><input type="checkbox"/>(선택) ML-DSA-65 키를 생성하고 파일 서명 결과 Verified OK를 확인했다.</label><label><input type="checkbox"/>(선택) 파일 변경 후 Verification failure를 확인했다.</label></div>
<div class="toolbar"><button id="resetChecks" type="button">체크 초기화</button></div>
</section><section id="refs"><div class="section-kicker">REFERENCES</div><h2>참고 문서</h2><p class="lead">PQC 추가 실습의 명령과 개념 설명은 아래 공식 문서를 기준으로 구성했습니다.</p><ul><li><a href="https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/10/html/securing_networks/creating-and-managing-tls-keys-and-certificates" rel="noopener" target="_blank">Red Hat Enterprise Linux 10 — Post-quantum cryptography algorithms in OpenSSL</a></li><li><a href="https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/10/html/10.2_release_notes/removed-features" rel="noopener" target="_blank">Red Hat Enterprise Linux 10.2 Release Notes — Non-post-quantum KEX removed from FUTURE</a></li><li><a href="https://csrc.nist.gov/pubs/fips/203/final" rel="noopener" target="_blank">NIST FIPS 203 — ML-KEM</a></li><li><a href="https://csrc.nist.gov/pubs/fips/204/final" rel="noopener" target="_blank">NIST FIPS 204 — ML-DSA</a></li></ul></section>
</main>
</div>
<script>
function escapeHtml(s){return s.replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));}
function highlight(code,lang){
  let lines=code.textContent.split('\n');
  code.innerHTML=lines.map(line=>{
    let e=escapeHtml(line);
    if(/^\s*#/.test(line)) return '<span class="com">'+e+'</span>';
    if(lang==='containerfile') e=e.replace(/^(FROM|ARG|RUN|COPY|LABEL|CMD|ENV|WORKDIR)\b/,'<span class="kw">$1</span>');
    else e=e.replace(/^(\s*)(sudo|export|printf|cat|echo|read|unset|rpm|uname|systemctl|sysctl|mkdir|grep)\b/,'$1<span class="cmd">$2</span>');
    e=e.replace(/(\$\{[^}]+\}|\$[A-Z_][A-Z0-9_]*)/g,'<span class="var">$1</span>');
    return e;
  }).join('\n');
}
async function copyText(text){
  // HTTPS/localhost 등 Clipboard API를 사용할 수 있는 환경
  if (navigator.clipboard && window.isSecureContext) {
    await navigator.clipboard.writeText(text);
    return;
  }

  // file:// 또는 Clipboard API가 제한된 환경용 fallback
  const ta = document.createElement('textarea');
  ta.value = text;
  ta.setAttribute('readonly', '');
  ta.style.position = 'fixed';
  ta.style.left = '-9999px';
  ta.style.top = '0';
  document.body.appendChild(ta);
  ta.focus();
  ta.select();

  const ok = document.execCommand('copy');
  document.body.removeChild(ta);

  if (!ok) throw new Error('copy command failed');
}

document.querySelectorAll('.code-card').forEach(card=>{
  // 툴팁/명령어 설명 안에도 <code>가 있으므로,
  // 복사·하이라이트 대상은 반드시 실제 명령어 영역(<pre><code>)으로 한정합니다.
  const code = card.querySelector('pre > code');
  if (!code) return;

  highlight(code, card.dataset.lang);

  // 화면 예시처럼 복사 버튼이 없는 code-card도 있으므로 안전하게 건너뜁니다.
  const button = card.querySelector('.copy');
  if (!button) return;

  button.addEventListener('click', async ()=>{
    const original = button.textContent;
    try {
      await copyText(code.textContent);
      button.textContent = '복사됨';
    } catch (e) {
      button.textContent = '복사 실패';
      console.error('Clipboard copy failed:', e);
    }
    setTimeout(()=>button.textContent = original, 1200);
  });
});
const boxes=[...document.querySelectorAll('.checklist input')];
function update(){const done=boxes.filter(x=>x.checked).length;document.querySelector('.progress>span').style.width=(done/boxes.length*100)+'%';document.querySelector('.progress-text').textContent=`${done} / ${boxes.length}`;boxes.forEach((b,i)=>localStorage.setItem('rhelLabCheck'+i,b.checked?'1':'0'));}
boxes.forEach((b,i)=>{b.checked=localStorage.getItem('rhelLabCheck'+i)==='1';b.addEventListener('change',update)});update();
document.getElementById('resetChecks').onclick=()=>{boxes.forEach(b=>b.checked=false);update();};
</script><script>
(function(){
  const saved = localStorage.getItem('rhel-lab-theme');
  const preferred = saved || (window.matchMedia && window.matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark');
  setTheme(preferred);
})();
function setTheme(theme){
  document.documentElement.setAttribute('data-theme', theme);
  try { localStorage.setItem('rhel-lab-theme', theme); } catch(e) {}
  const dark = document.getElementById('theme-dark');
  const light = document.getElementById('theme-light');
  if (dark && light) {
    dark.classList.toggle('active', theme === 'dark');
    light.classList.toggle('active', theme === 'light');
  }
}
</script><script>
document.querySelectorAll('.explain-toggle').forEach(button=>{
  button.addEventListener('click', ()=>{
    const panel = document.getElementById(button.getAttribute('aria-controls'));
    if (!panel) return;
    const open = !panel.classList.contains('open');
    panel.classList.toggle('open', open);
    button.setAttribute('aria-expanded', open ? 'true' : 'false');
    button.textContent = open ? '설명 닫기' : '명령어 설명';
  });
});

document.querySelectorAll('.quick-tip-wrap').forEach(wrap=>{
  const button = wrap.querySelector('.quick-tip');
  if (!button) return;
  button.addEventListener('click', (e)=>{
    e.stopPropagation();
    document.querySelectorAll('.quick-tip-wrap.open').forEach(other=>{
      if (other !== wrap) other.classList.remove('open');
    });
    wrap.classList.toggle('open');
  });
});
document.addEventListener('click', ()=>{
  document.querySelectorAll('.quick-tip-wrap.open').forEach(x=>x.classList.remove('open'));
});
</script></body></html>